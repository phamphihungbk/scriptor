version: '3'

networks:
  backend:
    driver: bridge
  frontend:
    driver: bridge

services:
  python:
    image: py-python
    build:
      context: ./python
    container_name: py-python
    working_dir: /web
    volumes:
    - ../web:/web
    - ../.env:/web/.env
    tty: true
    networks:
      - frontend
      - backend

  nginx:
    image: py-nginx
    build:
      context: ./nginx
    container_name: py-nginx
    working_dir: /web
    volumes_from:
      - php-fpm
    depends_on:
      - php-fpm
    ports:
    - 80:80
    - 443:443
    networks:
      - frontend

  mysql:
    image: py-mysql
    build:
      context: ./mysql
    container_name: py-mysql
    restart: always
    environment:
      - MYSQL_ROOT_PASSWORD=secret
    volumes:
    - ./mysql/docker-entrypoint-initdb.d:/docker-entrypoint-initdb.d
    - ./mysql/data:/var/lib/mysql
    networks:
      - backend

  phpmyadmin:
    image: py-mysql-admin
    build:
      context: ./phpmyadmin
    container_name: api-mysql-admin
    restart: always
    depends_on:
      - mysql
    environment:
      - PMA_HOST=api-mysql
      - PMA_ABSOLUTE_URI="${PHP_MY_ADMIN_DOMAIN}"
      - PMA_USER=root
      - PMA_PASSWORD=secret
    networks:
      - frontend
      - backend
